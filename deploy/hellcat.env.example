# hellcat ~/apps/clova-api-lab/.env — 백엔드 전용. 절대 커밋 금지(저장소 외부).
# 터널 전용이라 loopback 바인드.
PORT=3600
HOST=127.0.0.1

# CLOVA Studio 자격증명 (배포 후 실제 값 입력 → systemctl restart)
CLOVA_AUTH_MODE=legacy
CLOVA_BASE_URL=https://clovastudio.stream.ntruss.com
CLOVA_API_KEY=
CLOVA_APIGW_API_KEY=
# CLOVA_CHAT_PATH=
